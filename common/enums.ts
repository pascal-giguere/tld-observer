export enum TopicKey {
  newTlds = 'newTlds',
  upcomingTlds = 'upcomingTlds',
}

export enum ErrorCode {
  invalidData = 'invalidData',
  memberAlreadyExists = 'memberAlreadyExists',
  unknownError = 'unknownError',
}
